export enum ProjectDescription {
    Shop = ' React + redux-toolkit + redux-thunk + typescript + eslint/prettier',
    TodoList = ' React + redux + redux-thunk + typescript',
    TodoListToolkit = 'Migration to redux-toolkit',
    TodoListSaga = 'Migration to redux-saga',
    SocialNetwork = 'React(on class components) + redux + redux-thunk + typescript'
}